[package]
name = "defocus-interconnect"
version = "0.1.0"
edition = "2021"

[dependencies]
defocus-core = { path = "../defocus-core" }
serde_json = "1"
interconnect-core = { git = "https://github.com/rhi-zone/interconnect.git" }
